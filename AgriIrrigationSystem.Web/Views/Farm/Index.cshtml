@model IEnumerable<AgriIrrigationSystem.Web.Models.ViewModels.FarmViewModel>

@{
    ViewData["Title"] = "Farms List";
}

<h2>Farms List</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Farm</a>
</p>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Location</th>
            <th>Size (Hectares)</th>
            <th>Owner</th>
            <th>Crop Count</th>
            <th>Created At</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var farm in Model)
        {
            <tr>
                <td>@farm.Id</td>
                <td>@farm.Name</td>
                <td>@farm.Location</td>
                <td>@farm.Size</td>
                <td>@farm.OwnerName</td>
                <td>@farm.CropCount</td>
                <td>@farm.CreatedAt.ToString("yyyy-MM-dd")</td>
                <td class="text-center">
                    <a asp-action="Details" asp-route-id="@farm.Id" class="btn btn-info btn-sm me-2">
                        <i class="bi bi-eye me-1"></i> Details
                    </a>
                    <a asp-action="Edit" asp-route-id="@farm.Id" asp-route-returnUrl="@Url.Action("Index", "Farm")" class="btn btn-sm btn-primary">
                        <i class="bi bi-pencil-square"></i> Edit
                    </a>
                    <a asp-action="Delete" asp-route-id="@farm.Id" asp-route-returnUrl="@Url.Action("Index")" class="btn btn-sm btn-danger">
                        <i class="bi bi-trash"></i> Delete
                    </a>

                </td>


            </tr>
        }
    </tbody>
</table>
